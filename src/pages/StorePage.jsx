import { ArrowLeft, MapPin } from 'lucide-react'
import { useEffect, useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import EmptyState from '../components/EmptyState'
import Loader from '../components/Loader'
import ProductCard from '../components/ProductCard'
import { fetchProducts, fetchStore } from '../lib/catalog'
export default function StorePage(){const{slug}=useParams();const[store,setStore]=useState(null),[products,setProducts]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{fetchStore(slug).then(async s=>{setStore(s);if(s)setProducts(await fetchProducts({store:s.id}))}).finally(()=>setLoading(false))},[slug]);if(loading)return <Loader fullscreen/>;if(!store)return <main className="section-shell page-space"><EmptyState title="Boutique introuvable"/></main>;return <main><section className="store-hero"><div className="section-shell"><Link className="back-link" to="/stores"><ArrowLeft/> Boutiques</Link><div className="store-hero-content"><div className="store-logo-xl">{store.logo_url?<img src={store.logo_url} alt={store.name}/>:store.name.slice(0,2).toUpperCase()}</div><div><span className="eyebrow">Boutique One Market</span><h1>{store.name}</h1><p>{store.description||'Découvrez les produits de cette boutique.'}</p><span className="store-location"><MapPin/>{store.city||'RDC'}</span></div></div></div></section><section className="section-shell store-products"><div className="section-heading"><div><span>Catalogue vendeur</span><h2>{products.length} produit{products.length!==1?'s':''}</h2></div></div>{products.length?<div className="product-grid">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<EmptyState title="Aucun produit publié"/>}</section></main>}
